import React from 'react'

function Map_forEach() {
  function aaa(){
    console.log(3);
  }
  //반복문 : for, for of , foreach(), map()

  const arr = [1,3,5];
  
  //forEach() ()안에 내용을 반복 실행 배열에서만 사용가능
  arr.forEach((item,index) => {
    console.log(`a =${item}, b =${index}`);
  });


  //()안에 내용을 반복실행 !
  const result =  arr.map((i, a)=>{
    console.log(`i =${i}, a=${a}`);
    return i*2;
  });

console.log(result);

  return (
    <>

    <div>Map_forEach</div>
    <div></div>
    </>
  )
}

export default Map_forEach




























































































































































































































































































































































































































































































